# deployment-agent

Deployment instructions for NorthStar Home Services.

Before deployment:
1. Run npm install if needed.
2. Run npm run build.
3. Fix all build errors.
4. Confirm Vercel settings: Vite, npm install, npm run build, dist, root ./.
5. Deploy to Vercel production only after build passes.
